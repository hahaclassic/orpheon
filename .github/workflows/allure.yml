name: Run Go Tests with Allure

on:
  pull_request:
    branches:
      - develop
    paths:
      - 'backend/**'

jobs:
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 1.25
          cache: true
          cache-dependency-path: backend/go.sum

      - name: Install golurectl
        run: cd backend && go install github.com/robotomize/go-allure/cmd/golurectl@latest

      - name: Restore previous Allure history
        uses: actions/download-artifact@v4
        with:
          name: allure-history
          path: backend/allure-results/history

      - name: Run Unit Tests
        run: |
          cd backend
          mkdir -p allure-results/history
          go test -json ./internal/domain/... | golurectl -l -e -o allure-results --allure-suite Unit --allure-tags UNIT

      - name: Upload Unit Test Results
        uses: actions/upload-artifact@v4
        with:
          name: unit-allure-results
          path: backend/allure-results

      - name: Save Allure history
        uses: actions/upload-artifact@v4
        with:
          name: allure-history
          path: backend/allure-results/history

  integration-tests:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: unit-tests
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 1.25
          cache: true
          cache-dependency-path: backend/go.sum

      - name: Install golurectl
        run: cd backend && go install github.com/robotomize/go-allure/cmd/golurectl@latest

      - name: Restore previous Allure history
        uses: actions/download-artifact@v4
        with:
          name: allure-history
          path: backend/allure-results/history

      - name: Run Integration Tests
        run: |
          cd backend
          mkdir -p allure-results/history
          go test -json ./tests/integrations/... | golurectl -l -e -o allure-results --allure-suite Integration --allure-tags INTEGRATION

      - name: Upload Integration Results
        uses: actions/upload-artifact@v4
        with:
          name: integration-allure-results
          path: backend/allure-results

      - name: Save Allure history
        uses: actions/upload-artifact@v4
        with:
          name: allure-history
          path: backend/allure-results/history

  e2e-tests:
    name: E2E Tests
    runs-on: ubuntu-latest
    needs: integration-tests
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 1.25
          cache: true
          cache-dependency-path: backend/go.sum

      - name: Install golurectl
        run: cd backend && go install github.com/robotomize/go-allure/cmd/golurectl@latest

      - name: Restore previous Allure history
        uses: actions/download-artifact@v4
        with:
          name: allure-history
          path: backend/allure-results/history

      - name: Run E2E Tests
        run: |
          cd backend
          mkdir -p allure-results/history
          go test -json ./tests/e2e/... | golurectl -l -e -o allure-results --allure-suite E2E --allure-tags E2E

      - name: Generate Allure Report
        run: |
          cd backend
          allure generate allure-results --clean -o allure-report

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: backend/allure-report

      - name: Save Allure history
        uses: actions/upload-artifact@v4
        with:
          name: allure-history
          path: backend/allure-results/history
