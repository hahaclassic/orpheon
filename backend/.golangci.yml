version: 1

issues:
  exclude-dirs:
    - cmd/player

linters:
  enable-all: true

linters-settings:
  tagliatelle:
    case:
      rules:
        env: upperSnake
        json: snake_case

run:
  timeout: 1m
