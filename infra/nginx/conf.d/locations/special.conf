# 1. index /
location = / {
    index index.html;
}

# 2. /reserved/ 
location /reserved {
    try_files index.html =404;
}

# 3. /documentation
location /documentation {
    try_files /readme.html =404;
}

location /img/ {
    try_files $uri =404;
}

# 6. /managment
location /managment {
    try_files /managment.html =404;
}

# 4. Swagger UI
location /api/v1/ {
    proxy_pass http://swagger_ui/;  
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

# OpenAPI spec
location = /openapi/openapi.yml {
    try_files /openapi.yml =404;
}

location /monitoring/ {
    proxy_pass http://grafana:3000;

    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    proxy_redirect default;
}

# 5. Status 
location = /status {
    stub_status on;
    access_log off;
    allow 127.0.0.1;   # можно ограничить доступ локально
    deny all;
}

# upstream pgadmin {
#     server pgadmin:80; # контейнер pgAdmin
# }

# location /admin/ {
#         proxy_pass http://pgadmin/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;

#         # optional: авторизация nginx (базовая)
#         auth_basic "Restricted Admin";
#         auth_basic_user_file /etc/nginx/.htpasswd;
#     }
